//  ******Arrays******
//  Create a function that takes in an array of numbers, alert the sum.

let nums = [42, 12, 40, 2, 5, 6]

let sum = nums.reduce((acc, c) => acc + c, 0)
alert(sum)

let numz = ["5", 10, 12, 4]
let total = numz.reduce((acc, c) => acc + Number(c), 0)
alert(total)

// Create a function that takes in an array of numbers,
// return a new array of numbers that is every original number squared

// function newArr(nOne)

// Create a funtion that takes in a string
// Print the reverse of that string to the console

// Create a funtion that takes in a string
// Alert if the string is a palindrome or not